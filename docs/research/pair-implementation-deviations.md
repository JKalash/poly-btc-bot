# Pair implementation deviations ledger

Per spec §25.1 rule 10 (`docs/research/mrfadiai-polymarket-bot-borrow-implementation-spec.md`): every material deviation from the brief is recorded here with rationale, the invariant preserved, and the tests that cover it. Single writer: the implementation coordinator. Append-only.

| # | Spec reference | Deviation | Rationale | Invariant preserved | Tests |
|---|---|---|---|---|---|
| D-1 | §10.6, BPAIR-020 | Pair migration is `0006_*`, not the next-after-`0004` the brief's snapshot implied; upstream `0005_bitter_agent_brand.sql` (adds `positions.fees6`) landed before this work began. | Repository advanced past the brief's audited revision. | One forward migration for the pair schema; no history rewrite. | Migration tests (BPAIR-020). |
| D-2 | §6.5 | Local baseline is `4d955b0` (origin/main `2e395b2` + tutorial-seed fix), not `908a978` recorded in the brief. Batch-B live-path fixes (real live position ledger, content-seeded `idempotencyKey`, provenance/health fixes) and GitHub Actions CI are present. | Repository advanced during brief preparation and again during planning. | All §3 authority rules re-verified against the actual tree; pair isolation tests target the current live-path symbols. | BPAIR-003 guard tests; baseline doc. |
| D-3 | §18.1, §27.3, BPAIR-020 | Migration `0006` verified on PGlite only at implementation time; the PostgreSQL-mode run is outstanding because no container runtime (docker/podman/colima) exists on the development machine. | Environment limitation, not a design choice. The generated SQL uses no PGlite-specific constructs; the staged-upgrade test covers 0000–0005 → 0006 with populated legacy rows. | One additive forward migration; journal untouched by hand. To close: run `DATABASE_URL=<pg> pnpm --filter @b5p/db migrate` against real PostgreSQL (e.g. `pnpm infra:up` on a Docker-capable host) before any production deploy. | `packages/db/test/migration.test.ts` (13 tests, PGlite). |
| D-4 | §18.9, §18.4.1 | `pair_action_intents`' composite key `(id, group_id, action_sequence)` is a table-level `unique()` constraint rather than a `uniqueIndex`, because drizzle-kit 0.30.1 emits ALTER-ADD-FK before standalone CREATE INDEX, breaking composite FKs that target an index. | Toolchain ordering bug, reproduced in PGlite before the fix. | Same uniqueness guarantee, same name; composite FK from `pair_effect_outbox` intact. | Migration test asserts FK + constraint in catalog. |
| D-5 | §18.11.1 | `pair_research_artifacts` gained an extra partial unique `(run_id, artifact_kind) WHERE scenario_id IS NULL` beyond the spec'd three-column unique, because PostgreSQL treats NULL scenario_id rows as distinct. | Spec's uniqueness intent would otherwise be unenforced for run-level artifacts. | Stricter than spec (§3 conflict rule: stricter wins). | Migration test. |
| D-6 | §15.7, BPAIR-038 | The durable event vocabulary adds schema-v1 `PAIR_SETTLEMENT_STARTED` and `PAIR_SETTLEMENT_DEFERRED` events beyond the minimum list in the brief. | The listed vocabulary had no event capable of reaching `AWAITING_SETTLEMENT` or moving a hold-policy paired group to `AWAITING_RESOLUTION`; leaving those states unreachable would make the normative transition table internally incomplete. | Settlement remains explicit, causal, immutable, and versioned; no economic effect is introduced by either routing event. | Reducer legal-matrix snapshot, settlement/hold transition tests, invariant suite. |
| D-7 | §10.1, BPAIR-061 | The engine account adapter imports ledger construction through the explicit non-barrel subpath `@b5p/pair-execution/internal/ledger`; the public package barrel still does not export mutation helpers. | The Drizzle adapter must persist and reconstruct the pure package's exact journal/lot contracts without duplicating their definitions. Package `exports` makes the exception visible and narrowly scoped instead of allowing arbitrary source-path imports. | Common callers still see only immutable public contracts/economics; no reducer or ledger mutation helper is available from `@b5p/pair-execution`. The account adapter remains paper-only and has no venue/live dependency. | Pair capability guard (11), ledger tests (8), pair-account PGlite tests (3). |
| D-8 | §13.5, BPAIR-071 | Activation-only causality/policy/rules/permit failure codes are defined locally in `pair-activation.ts` rather than widening the shared `PairRejectionCode` union during the pure-core slice. | The normative rejection list includes fee/constraint/quote activation codes but omits stable names for cutoff causality, policy/rules identity, signal-size authority, and permit expiry. Collapsing them into unrelated codes would lose audit meaning. | Every activation failure is still an immutable typed rejection; shared economic/risk reasons retain their canonical codes, and no effect is created on rejection. | Pair activation tests (7). |
| D-9 | §10.1, BPAIR-073 | Pure serial-dispatch planners are available to the engine coordinator only through `@b5p/pair-execution/internal/serial-dispatch`, not the public barrel. | The adopted facade delegates durable due-work preparation to an injected activation port, whose engine adapter needs the exact symmetric planner without widening the common-caller API. | Public callers still see only the six-method facade; serial planners remain pure, paper-only, and unavailable from the root barrel. | Serial dispatch tests (13), facade boundary tests (13), capability guard (11). |
| D-10 | §18.12, BPAIR-064 | For a pair account with zero groups, account-only reconciliation diff details are retained in the nullable-group reconciliation summary rather than child `pair_reconciliation_diffs` rows. | The migrated child table requires non-null `group_id` with a group foreign key, so inventing a fake group would corrupt causality. The parent reconciliation row legitimately permits `group_id = null`. | Zero-group accounts are still independently replayed, durably audited, and fail paper scheduling closed on any drift; no ledger/event truth is fabricated. | Startup reconciliation tests (7), including healthy and drifted zero-group accounts. |
| D-11 | §10.1, BPAIR-075 | Pure settlement integration planners are exposed only through `@b5p/pair-execution/internal/settlement-integration`, not the public root barrel. | The engine persistence adapter needs exact merge/resolution consumptions and balanced journals while the common facade must remain narrow and must not expose ledger mutation. | Public callers retain only the six-method facade; settlement integration remains deterministic, paper-only, and unavailable from the root barrel. | Settlement integration tests (11), settlement core tests (10), capability guard (11). |
