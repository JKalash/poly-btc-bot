# Pair implementation deviations ledger

Per spec §25.1 rule 10 (`docs/research/mrfadiai-polymarket-bot-borrow-implementation-spec.md`): every material deviation from the brief is recorded here with rationale, the invariant preserved, and the tests that cover it. Single writer: the implementation coordinator. Append-only.

| # | Spec reference | Deviation | Rationale | Invariant preserved | Tests |
|---|---|---|---|---|---|
| D-1 | §10.6, BPAIR-020 | Pair migration is `0006_*`, not the next-after-`0004` the brief's snapshot implied; upstream `0005_bitter_agent_brand.sql` (adds `positions.fees6`) landed before this work began. | Repository advanced past the brief's audited revision. | One forward migration for the pair schema; no history rewrite. | Migration tests (BPAIR-020). |
| D-2 | §6.5 | Local baseline is `4d955b0` (origin/main `2e395b2` + tutorial-seed fix), not `908a978` recorded in the brief. Batch-B live-path fixes (real live position ledger, content-seeded `idempotencyKey`, provenance/health fixes) and GitHub Actions CI are present. | Repository advanced during brief preparation and again during planning. | All §3 authority rules re-verified against the actual tree; pair isolation tests target the current live-path symbols. | BPAIR-003 guard tests; baseline doc. |
| D-3 | §18.1, §27.3, BPAIR-020 | Migration `0006` verified on PGlite only at implementation time; the PostgreSQL-mode run is outstanding because no container runtime (docker/podman/colima) exists on the development machine. | Environment limitation, not a design choice. The generated SQL uses no PGlite-specific constructs; the staged-upgrade test covers 0000–0005 → 0006 with populated legacy rows. | One additive forward migration; journal untouched by hand. To close: run `DATABASE_URL=<pg> pnpm --filter @b5p/db migrate` against real PostgreSQL (e.g. `pnpm infra:up` on a Docker-capable host) before any production deploy. | `packages/db/test/migration.test.ts` (13 tests, PGlite). |
| D-4 | §18.9, §18.4.1 | `pair_action_intents`' composite key `(id, group_id, action_sequence)` is a table-level `unique()` constraint rather than a `uniqueIndex`, because drizzle-kit 0.30.1 emits ALTER-ADD-FK before standalone CREATE INDEX, breaking composite FKs that target an index. | Toolchain ordering bug, reproduced in PGlite before the fix. | Same uniqueness guarantee, same name; composite FK from `pair_effect_outbox` intact. | Migration test asserts FK + constraint in catalog. |
| D-5 | §18.11.1 | `pair_research_artifacts` gained an extra partial unique `(run_id, artifact_kind) WHERE scenario_id IS NULL` beyond the spec'd three-column unique, because PostgreSQL treats NULL scenario_id rows as distinct. | Spec's uniqueness intent would otherwise be unenforced for run-level artifacts. | Stricter than spec (§3 conflict rule: stricter wins). | Migration test. |
